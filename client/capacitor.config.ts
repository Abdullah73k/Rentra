import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
	appId: "com.abdullah.propertymanagement",
	appName: "property-management",
	webDir: "dist",
};

export default config;
